---
title: Configuration
description: Default configuration for EzDonutStore
---

```yaml
link:
  raw: "https://store.donutsmp.net"
  display: "store.donutsmp.net"

message:
  store:
    enabled: true
    lines:
      - "&#00A4FCSupport DonutSMP on the official store!"
      - "&#00A4FC&l*&r &f&n%link_display%"
    hover:
      - "&#00A4FCOpen the server store"

  actionbar:
    enabled: true
    text: "&7The store link has been sent in chat!"

  reload:
    enabled: true
    lines:
      - "&#00A4FC&l*&r &f&nReloaded the config!"

sounds:
  pling:
    enabled: true
    sound: BLOCK_NOTE_BLOCK_PLING
    volume: 0.5
    pitch: 1.0

placeholderapi:
  enabled: true
```
